import React from 'react';
import { Text } from 'react-native';
import { Header } from './common';

const ProjectHeader = () => {
  return (
    <Header>MetWorkz</Header>
  );
};

export default ProjectHeader;
